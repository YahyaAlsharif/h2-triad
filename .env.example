# Optional Docker Compose override. Keep the database inside the mounted data directory.
DATABASE_PATH=/app/data/app.db
# Applied only when initializing a new database. Existing records are never reseeded.
SEED_DEMO_DATA=true
